// This is for themonth display

function OneContainer() {
    var OneContainer = document.querySelector(".play-img-one");
    OneContainer.style.display = OneContainer.style.display === "block" ? "none" : "block";
}